class Solution:
	def reverse_digit(self, n):
		# Code here
		rev=0
		while(n>0):
		    x=n%10
		    rev=rev*10+x
		    n=n//10
		return rev

Method 2:
n=int(input("Enter the Number: "))
N=str(n)
print(int(N[::-1]))
